## このリポジトリでしたいこと
ブラウザ起動時に展開する、入力フォームと検索開始ボタンを備えたスタートページ。
背景にthree.jsシーンを配置し、カーソル位置に反応するanime.jsの演出でビジュアルを向上させる、
ブラウザの利用体験を上げるアプリ。

## 要素
- 入力フォーム（テキスト入力欄）
- 検索開始ボタン
- 背景の3Dシーン（カーソル位置に反応するオブジェクト）

## 技術方針

### 役割分担
- **起動方法**: 拡張機能は使わず、生成した静的htmlファイルをブラウザの「ホームページ/起動時に開くページ」に
  ユーザー自身が設定して運用する。ファイル単体で完結するため、manifest.json等の拡張機能特有の構成は不要。
- **検索**: フォームをsubmitすると、入力値をクエリパラメータとして外部検索エンジン（Google等）のURLへ
  遷移する。フォームの`action`/`method`とURL組み立てのみで完結し、独自の検索ロジックは実装しない。
- **カーソル反応演出**: [app-animejs-adapter.md](./app-animejs-adapter.md)の対応表カテゴリD
  （クリック/ホバーへの反応演出）に該当する。マウス座標から対象オブジェクトを特定し（Raycaster、
  または単純な座標マッピング）、`animate(mesh/material, {...})`で確定値へ補間するだけで済むため、
  毎フレームの物理演算は不要。

### file://直接オープンに関する制約（検証済み）
- **ローカルサーバーの起動は不要**: `examples/03-animejs-playground.html`を実際にfile://で直接開いて
  検証した結果、`<script type="module">`＋importmapでCDN（unpkg等）からthree.js/anime.jsを読み込む
  構成はfile://でも問題なく動作する（リモートhttpsへのESM importはCORSでブロックされない）。
  そのためUMD/グローバル版への切り替えは不要で、これまで通りESM importmap方式を採用する。
- **ローカル相対パスのアセットfetchは避ける**: 一方で、`TextureLoader`等で相対パスのローカル画像
  （例: `./src/xxx.png`）を読み込もうとすると、file://では各ファイルが別オリジン扱いとなり
  `Access to image at 'file:///...' from origin 'null' has been blocked by CORS policy`で失敗する
  ことを確認済み。このアプリでは画像テクスチャのような相対パスアセットのfetch読み込みは行わず、
  背景の3Dオブジェクトは色・ジオメトリ等の手続き的なマテリアルで構成する（どうしても画像が必要な
  場合はbase64のdata URIとしてhtml内に埋め込む）。

### 要素ごとの実装方法
| 要素 | 実装方法 |
|---|---|
| 入力フォーム・検索開始ボタン | 標準の`<form>`/`<input>`/`<button>`。submit時に外部検索エンジンのURLへ遷移 |
| 背景の3Dシーン | three.jsで簡易なオブジェクト（複数）をシーンに配置し、常時レンダリングループで描画 |
| カーソル反応演出 | `mousemove`イベントで座標を取得し、対応するオブジェクトを特定後、animejs+three.jsアダプター経由で`animate()`により色・スケール・位置などを確定値へ補間 |

## 未確定事項（後続の検討）
- 背景オブジェクトの種類・数・配置（幾何学的な図形、パーティクル群など）
- カーソル反応の種類（追従、拡大縮小、色変化、複数の組み合わせなど）
- フォーム・ボタンのデザイン方向性（3Dシーンに重ねるUIとしての見た目）
- 検索エンジンの既定値・切り替えの要否
